module Puriq.EvaluatorSpec (spec) where

import Test.Hspec
--import Test.QuickCheck
--import Puriq.Types

spec :: Spec
spec = do
  describe "Evaluator" $ do
    describe "evaluación básica" $ do
      it "evalúa literales enteros" $ do
        --True `shouldBe` True
        pending
        
      it "evalúa operciones aritméticas" $ do
        --True `shouldBe` True
        pending
      
